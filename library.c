#include <stdio.h>
#include <stdlib.h>
#include "linkedlist.h"
#include "library.h"

void addSong(struct song_node *snode, char *sartist, char *sname) {
  // snode[0] = insert_inorder(snode[0], sartist, sname);
}
